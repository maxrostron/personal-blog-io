import format from "date-fns/format";
import { navigate } from "hookrouter";

<div className="blog__article-header">
  <span
    className="blog__article-header-category"
    onClick={() => navigate("http://localhost:3000/blog")}
  >{`blog//${props.article.meta.category}`}</span>
  <h1 className="blog__article-header-title">{props.article.meta.title}</h1>
  <span className="blog__article-header-date">
    {format(new Date(props.article.meta.date), "do MMMM yyyy")}
  </span>
</div>
